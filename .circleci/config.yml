# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs:
  buildpacks/pack@dev:4fd8739
workflows:
  main:
    jobs:
      - buildpacks/build:
          image-name: nodefun 
          builder: 'heroku/buildpacks:18'
